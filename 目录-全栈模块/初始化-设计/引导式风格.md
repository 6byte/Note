## 引导式风格

#### 风格-注释

```
/**
	功能：
		功能1:判断是否xxx
		功能2:删除xxx
	参数:
		参数1(str):需要处理的xxx
		参数2(obj)：需要渲染的对象
	返回值:
    	result.str:过滤后的xx
		result.status:
			值为true:有非法字符
			值为false:没有非法字符
	使用注意：
		不可以传入数组
*/

```

